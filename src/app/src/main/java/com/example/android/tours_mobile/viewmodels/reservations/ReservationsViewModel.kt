package com.example.android.tours_mobile.viewmodels.reservations

import androidx.lifecycle.ViewModel

class ReservationsViewModel : ViewModel(){
    /*
    var reservations = MutableLiveData<List<ReservationDTO>>()
    var reservationRepository = reservationRepository()

    init {
        reservations = reservationRepository.reservations
    }

    fun getReservations(user : UserDTO){
        reservationRepository.getReservations(user)
    }

    */
}