package com.example.android.tours_mobile.viewmodels.explore

import androidx.lifecycle.ViewModel

class ExploreViewModel : ViewModel(){
    /*
    var tours = MutableLiveData<List<ReservationDTO>>()
    var toursRepository = toursRepository()

    init {
        tours = toursRepository.reservations
    }

    fun searchTours(place : String, departure : String, arrival : String, user : UserDTO?){ // user can be log or not
        toursRepository.getTours(place, departure, arrival, user)
    }

    */
}