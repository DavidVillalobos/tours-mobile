package com.example.android.tours_mobile.viewmodels.favorites

import androidx.lifecycle.ViewModel

class FavoritesViewModel : ViewModel(){
    /*
    var toursFavorites = MutableLiveData<List<ToursDTO>>()
    var toursRepository = ToursRepository()

    init {
        toursFavorites = toursRepository.toursFavorites
    }

    fun getFavorites(user : UserDTO){
        toursRepository.getFavoritesTours(user)
    }

    */
}